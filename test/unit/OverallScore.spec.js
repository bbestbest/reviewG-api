// 'use strict'

// const { test } = use('Test/Suite')('OverAllScore')
// const overallScore = require('../../util/OverAllScoreUtil')

// test('Overall score should return float number', async ({ assert }) => {
//   const story = 1
//   const gameplay = 2
//   const performance = 3
//   const graphic = 4
//   const target = (story+gameplay+performance+graphic)/4
//   const overall = await overallScore(story,gameplay,performance,graphic)
//   assert.equal(target,overall)
// }),
// test('Overall score should return a', async ({ client }) => {
//   const user = {

//   }
// })